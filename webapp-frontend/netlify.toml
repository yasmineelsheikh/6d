[build]
  command = "npm run build"
  publish = ".next"

# Proxy API requests to Vercel backend
[[redirects]]
  from = "/api/*"
  to = "https://6d-nu.vercel.app/api/:splat"
  status = 200
  force = true
